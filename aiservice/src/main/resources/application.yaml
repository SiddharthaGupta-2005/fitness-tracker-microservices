server:
  port: 8083

spring:
  application:
    name: ai-service
  main:
    banner-mode: off
  config:
    import: optional:configserver:http://localhost:8888
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb+srv://fittracker_admin:h05RtNwGAw8v9xx3@fitness-cluster.41bmibj.mongodb.net/fitnessrecommendation?retryWrites=true&w=majority&appName=fitness-cluster}
      database: fitnessrecommendation
  mongodb:
    uri: ${SPRING_DATA_MONGODB_URI:mongodb+srv://fittracker_admin:h05RtNwGAw8v9xx3@fitness-cluster.41bmibj.mongodb.net/fitnessrecommendation?retryWrites=true&w=majority&appName=fitness-cluster}
    database: fitnessrecommendation
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: ${SPRING_RABBITMQ_USERNAME:guest}
    password: ${SPRING_RABBITMQ_PASSWORD:guest}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

rabbitmq:
  exchange:
    name: fitness.exchange
  queue:
    name: activity.queue
  routing:
    key: activity.tracking

openrouter:
  api:
    url: ${OPENROUTER_API_URL:https://openrouter.ai/api/v1/chat/completions}
    key: ${OPENROUTER_API_KEY:}
    model: ${OPENROUTER_MODEL:openrouter/free}